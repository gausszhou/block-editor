{
  "name": "block-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest watch",
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @block-editor/demo dev"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "happy-dom": "^20.8.9",
    "prettier": "^3.5.3",
    "typescript": "~5.8.3",
    "vitest": "^4.1.2"
  }
}
